float StopDistance = -5.0f; // Example value for StopDistance

if (StopDistance < 0.f) {
    // Execute code if StopDistance is less than zero
    std::cout << "StopDistance is negative." << std::endl;
} else {
    // Execute code if StopDistance is zero or positive
    std::cout << "StopDistance is zero or positive." << std::endl;
}
